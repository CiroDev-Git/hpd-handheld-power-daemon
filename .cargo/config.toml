# .cargo/config.toml
#
# Workspace-wide lint policy moved to `[workspace.lints]` in the root
# Cargo.toml (see Lote 34). This file is intentionally kept around as
# a parking spot for cargo-level config that does need to live here
# (e.g. build target rustflags, registry mirrors); it carries no
# active settings today.
